# Handoff — AI Native Testing Platform (V2.0 L2 全面加固计划 v2)

**日期**: 2026-06-01
**Branch**: `dev/phase1-implementation`
**Last commit**: `cb8c8ab` — docs(layer2): V2.0 L2 全面加固计划落盘

**Working dir**: `C:\Users\17381\Desktop\test_agent`

---

## 1. 一句话总结

L1 V1.7 (b16ffe8) + 收尾 (c2e5a2f) + V2.0 计划 v1 (cb8c8ab) 完成。**本次修订**：根据 GPT 外部评审插入 Phase 1.6（N2 + planning_graph explore + SystemMap 三件套加固），V2.0 变 5 阶段 / 5 commit / 9-10 天。L1 + Phase 1.5 + L2 全部加固路径已落盘。

## 2. 接手后第一件事

跑 smoke test 确认 V1.7 + V2.0 计划落盘没回归：

```powershell
cd C:\Users\17381\Desktop\test_agent
$env:PYTHONIOENCODING = "utf-8"
python -m pytest tests/core/test_l1_prompts.py tests/core/test_phase15_prompts.py -v
```

期望：**53 passed, 8 skipped in ~1s**（8 skipped 是 `L1_LIVE=1` 才跑的 live 测试）。

确认 V2.0 计划文档存在：
```bash
ls docs/layer2-v2.0-plan.md docs/devlog/21-layer2-v2.0-plan.md docs/handoff/
```

期望：3 个文件都在。

## 3. 本次 session 做了什么

按时间顺序：

1. **V1.7 收尾 (c2e5a2f)**：写 devlog 20 + 更新 progress + 更新 CONTEXT
2. **V2.0 计划 v1 (cb8c8ab)**：写主计划 + devlog 21 + 更新 progress + 更新 CONTEXT + 更新 prompt-engineering.md (§8 L2 草案)
3. **GPT 外部评审核对**：通过实际代码 grep 验证 GPT 判断（SystemMap V1.2 已存在，不是"缺"）
4. **V2.0 计划 v2 修订（本次）**：
   - 主计划加 Phase 1.6 (1.6.1 N2 fallback / 1.6.2 explore V1.6 / 1.6.3 SystemMap 采样 / 1.6.4 文档)
   - devlog 21 同步
   - progress 加步骤 22 (Phase 1.6)
   - CONTEXT 加 Phase 1.6 路线 + 引用 GPT 讨论
   - 写本 handoff

## 4. 关键产出

### 新文件
- `docs/layer2-v2.0-plan.md` — V2.0 主计划（v2，11 节，10 天）
- `docs/devlog/21-layer2-v2.0-plan.md` — V2.0 规划 devlog（v2）
- `docs/handoff/2026-06-01-layer2-v2.0-plan-with-phase16.md` — 本 handoff

### 关键修改
- `docs/devlog/00-progress.md` — 加步骤 21 (V2.0 计划) + 步骤 22 (Phase 1.6)
- `CONTEXT.md` — V1.7 + V2.0 + Phase 1.6 路线图完整记录
- `docs/prompt-engineering.md` — §8 L2 模板草案（待 V2.0 B 实施时再扩）

## 5. V2.0 计划最终结构（v2）

| 阶段 | 名称 | 工时 | Commit |
|---|---|---|---|
| **Phase 1.6** | N2/explore/SystemMap 三件套 | 1.5-2d | `fix(layer1): Phase 1.6 ...` |
| **Phase A** | L2 安全网 + 测试基础设施 | 1.5d | `fix(layer1+layer2): L2 safety net ...` |
| **Phase B** | L2 Prompt V1.6 化 | 2.5d | `feat(layer2): L2 prompts V1.6 ...` |
| **Phase C** | 联动 L1 业务模型 | 1d | `feat(layer2): L1→L2 linkage ...` |
| **Phase D** | L2 可观测性 | 1d | `feat(layer2): observability` |
| **合计** | 5 阶段 / 5 commit | **9-10d** | |

**执行顺序**：1.6 → A → B → C → D（修理论模型不稳再搞执行层）

## 6. 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 范围 | 5 阶段全做（不做 Phase E Reflection） | Phase E 需 50 case 数据评估 ROI |
| 测试 | `test_l2_prompts.py` + `L2_LIVE` + `scratch/test_l2_e2e.py` | 三件套覆盖单元 + 集成 + e2e |
| evaluate_js 沙箱 | 5 关键字黑名单 | 轻量 + 影响面小 |
| 合并 | 阶段独立 commit（5 个） | PR 评审清晰 + rollback 容易 |
| Gap Analyzer | **V2.1 候选**（不在 V2.0） | GPT 新建议最有价值，但与 L2 加固正交 |
| 执行顺序 | **1.6 先于 A** | V1.7 报告点名的 P0 漏洞在 1.6 修 |

## 7. 不在 V2.0 范围

- ❌ Phase E Reflection（50 case 数据评估）
- ❌ Gap Analyzer（V2.1）
- ❌ Business Graph / LangSmith / Multi-Agent
- ❌ 改 tools.py 14 工具（除 evaluate_js）
- ❌ checkpointer 升级
- ❌ tools.py 工具面缺陷（V19-V22）

## 8. 下一步（按 ROI）

### P0 — Phase 1.6.1 (修 N2 SystemModel fallback)
- 排查 4 fixture live test 中 N2 fallback 具体原因
- 加固 prompt：把"3 选 1 判定"明确化
- 跑 4 fixture live 验证 fallback 触发率从 3/4 → ≤ 1/4

### P1 — Phase 1.6.2 (planning_graph explore V1.6 化)
- explore_decide 改 5 段 XML + Output Contract
- explore_execute 加 inter-node 契约
- `<context>` 注入 SystemModel + ExplorationHistory + Goals

### P2 — Phase 1.6.3 (SystemMap 采样 + 测试)
- 10/15 → 20/30
- 加 `tests/core/test_system_mapper.py`
- Invariant: 3 字段非空时 LLM 提取稳定性 ≥ 80%

### P3 — V2.1 Gap Analyzer
- SystemModel vs SystemMap 比对
- 输出"疑似功能缺失"
- GPT 2026-06-01 建议

## 9. 验证清单（新对话必跑）

### Step 1 — git status（30s）
```powershell
git status        # 期望: clean
git log --oneline -5
# 期望: cb8c8ab → c2e5a2f → b16ffe8 → 65324a0
```

### Step 2 — 单元测试（5s, 不消耗 token）
```powershell
$env:PYTHONIOENCODING = "utf-8"
python -m pytest tests/core/test_l1_prompts.py tests/core/test_phase15_prompts.py -v
```
期望：**53 passed, 8 skipped**

### Step 3 — 确认 V2.0 计划文档完整（10s）
```powershell
ls -la docs/layer2-v2.0-plan.md docs/devlog/21-layer2-v2.0-plan.md
# 期望: 2 个文件存在
```

### Step 4 — L1 smoke test（2-3 min, 消耗 token）
```powershell
$env:L1_LIVE = "1"
python -m pytest tests/core/test_l1_prompts.py::test_l1_live_all_fixtures[prd_purchase] -v -s
```
期望：PASSED in ~2min，5 rules / 5 use_cases / 100% covered

## 10. 环境 & 配置

继承 V1.7 handoff（cb8c8ab 落盘后未改 `.env`）：

- `ANTHROPIC_MODEL=mimo-v2.5`（全部 4 个 model_type）
- `ANTHROPIC_BASE_URL=https://token-plan-cn.xiaomimimo.com/anthropic`
- `DATABASE_URL=postgresql://postgres:123456@localhost:5432/smart_test`
- `BACKEND_PORT=8002` / `FRONTEND_PORT=5173`
- `MAX_STEPS_PER_CASE=15` / `MAX_CONSECUTIVE_FAILURES=3`
- `MAX_EXPLORE_PAGES=3` / `MAX_EXPLORE_MINUTES=1`

⚠️ Windows 终端运行 Python 含中文/emoji 必须设：
```powershell
$env:PYTHONIOENCODING = "utf-8"; $env:PYTHONUTF8 = "1"; [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

## 11. 已知坑（务必避开）

### LLM 输出格式
- mimo-v2.5 / qwen / deepseek / kimi 在 `with_structured_output` 上**依旧不稳定**
- V1.7 把 3 个 Phase 1.5 skill 统一走 `safe_structured_invoke`
- V2.0 B 阶段会把 L2 assert 也切到 `safe_structured_invoke`
- **不要再在任何 skill 里用裸 `llm.ainvoke(prompt).content`**

### V1.7 护栏（破坏会回退）
- `severity` ∈ {high, medium, low}
- scenario id 格式 `S-NNN`
- summary ≤ 100 字
- status 透传

### 测试基础设施
- `tests/conftest.py` 已加 `load_dotenv()`，**别删**
- 4 个 L1 live 测试默认 skip，必须 `L1_LIVE=1` 才跑
- V2.0 A 阶段会加 `L2_LIVE=1` 开关

### 新坑（V2.0 B 阶段新护栏）
- assert `status ∈ {PASS, FAIL, INCONCLUSIVE}`
- assert `reasoning ≤ 200 字`
- assert `confidence ∈ {high, medium, low}`
- decide **必填 tool_call**（含 mark_task_*）或显式 stop

### 死代码 / 历史
- `goal_extractor.py` V1.5 已删死代码（50+ 行）
- `_FAST_PATH_COVERAGE_THRESHOLD = 0.9` 是 defensive safety net
- `system_mapper.py` 采样参数 v1 (10/15) → v1.6 (20/30)

### PowerShell & Git
- PowerShell 不支持 `&&`，用 `cmd1; if ($?) { cmd2 }`
- 长 commit message 含 `>` / `<` 会被 shell 吞，用 `git commit -F <file>`
- 含中文 commit message 用 `[System.IO.File]::WriteAllText` + `UTF8Encoding($false)`

## 12. 必读文档（按顺序）

1. **`docs/layer2-v2.0-plan.md`** — **本次 v2 主计划，必读**
2. `docs/l1-verification-report-v1.7.md` — L1 + Phase 1.5 验证报告
3. `docs/prompt-engineering.md` — V1.6 5 段 XML 模板 + L1↔L2 契约 + L2 草案（§8）
4. `docs/devlog/20-layer1-prompt-engineering-v17.md` — V1.6 + V1.7 L1 收尾
5. `docs/devlog/21-layer2-v2.0-plan.md` — V2.0 规划 devlog（v2 修订版）
6. `docs/devlog/16-phase2-v1.1-v1.2-graph-refactor.md` — V1.2 SystemMapper 引入（理解 Phase 1.6 背景）
7. `CONTEXT.md` — 项目全局状态
8. `tests/core/test_l1_prompts.py` — L1 7 个 invariant 模板
9. `core/skills/system_mapper.py` — Phase 1.6 改造目标
10. `agents/ui/planning_graph.py:288-325` — Phase 1.6.2 改造目标（generate_system_map_node / extract_scenarios_node）

## 13. GPT 评审关键洞察（重要）

**GPT 2026-06-01 建议**（已部分采纳）：

1. ✅ "L1 已经稳定" → 同意
2. ✅ "N2 SystemModel 是 L1 最薄弱节点" → **采纳为 Phase 1.6.1**
3. ❌ "缺 actual_pages/actions/forms" → **不准确**，SystemMap V1.2 已存在
4. ✅ "补理论 + 真实桥梁" → **采纳为 Phase 1.6 (1.6.2 + 1.6.3)**
5. ⭐ **"建 Gap Analyzer（SystemModel vs SystemMap 比对）"** → **V2.1 候选**，本计划 V2.0 不做

**吸取教训**：V1.7 报告里"3/4 fixture fallback"的数据**已经写在文档里**，但 V2.0 v1 落盘时**没把它升级为 P0**。GPT 提醒后才意识到。后续每份报告里的"P1 验证报告判断"都应在计划阶段就升级为 P0。

## 14. 完整改动统计

```
v15 handoff  → v16: 5 L1 skill 加固
v16         → v17: 3 Phase 1.5 skill 加固 + L1 盲点 B/C 修复
v17         → c2e5a2f: L1 收尾 (devlog 20)
c2e5a2f     → cb8c8ab: V2.0 计划 v1 落盘 (4 阶段)
cb8c8ab     → 本次: V2.0 计划 v2 修订 (5 阶段，加 Phase 1.6)
```

| 指标 | v1.7 | v2.0 v1 (cb8c8ab) | v2.0 v2 (本次) |
|---|---|---|---|
| 加固的 skill/prompt 数 | 8 (L1 + Phase 1.5) | 3 (L2 计划) | 3 + 3 (L2 + planning_graph explore + SystemMap) |
| 回归测试数 (mock) | 53 | 53 | 53 + 24-32 (L2) + 4-8 (SystemMap) |
| 回归测试数 (live skip) | 8 | 8 | 8 + 4 (L2) + 4 (SystemMap) |
| 计划文档 | 0 | 1 (V2.0) | 1 (V2.0 v2 + 修订记录) |
| 联动深度 | L1 → Phase 1.5 | L1 → L2 (单向) | L1 → L2 + planning_graph 协同 |
| 可观测性 | execution_logger | + token + WS | + SystemMap 质量监控 |

## 15. 紧急联系

如果新对话遇到无法解决的问题：

1. `git log --oneline -5` 确认在 `cb8c8ab` 或本次修订 commit
2. `python -m pytest tests/core/test_l1_prompts.py tests/core/test_phase15_prompts.py -v` 确认 53 passed
3. 单步调试 L2：
   ```python
   import asyncio
   from agents.ui.execution_graph import decide_node
   state = {"messages": [...], "task_config": {...}}
   print(asyncio.run(decide_node(state)))
   ```
4. 单步调试 SystemMap：
   ```python
   import asyncio
   from core.skills.system_mapper import generate_system_map
   history = [{"url": "http://...", "interactive_elements": [...]}]
   print(asyncio.run(generate_system_map(history)))
   ```
5. 看 `docs/layer2-v2.0-plan.md` §10 修订记录

---

**Good luck. V2.0 v2 plan is solid. Focus next session on Phase 1.6.1 — fixing N2 SystemModel fallback. The earlier fallback is reduced (from 3/4 to ≤ 1/4), the more reliable the entire downstream (SystemMap / scenario_extractor / explore_decide) becomes.**
