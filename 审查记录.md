# M2 审查记录

**日期**: 2026-06-10
**审查者**: Claude Code (plan-reviewer)
**范围**: M2 Runtime explore/execute 拆分 + CandidateCase 目标驱动执行

---

## 审查结果

### P0 问题

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | GoalResult 缺少 schema_version 默认值 | ✅ 已修复 | 默认值为 "goal_result.v1" |
| 2 | RuntimeExecutableCase 缺少 required_roles 字段 | ✅ 已修复 | CandidateTestCase 已添加 required_roles 字段 |
| 3 | adapt_executable_cases 语法错误 | ✅ 已修复 | 修复 list comprehension 语法 |

### P1 问题

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | _extract_role 角色匹配顺序不当 | ✅ 已修复 | 改为 list of tuples，先检查更具体的匹配 |
| 2 | StructuredPrecondition 缺少 failure_policy 默认值 | ✅ 已修复 | 默认值为 "incomplete" |
| 3 | ExplorationResult 缺少 system_map 默认值 | ✅ 已修复 | 使用 SystemMapEvid() 默认工厂 |

### 设计决策

| 决策 | 结论 |
|------|------|
| RuntimeExecutableCase 字段映射 | 严格保持 CandidateTestCase 的 id/goal/expected_result 不变 |
| 前置条件解析 | 基于关键词检测，无法确定时默认 business_state |
| 终态判定 | 三条件全部满足才判定 passed |
| 浏览器状态策略 | 同角色复用登录态，跨角色清理 Cookie/storage |

### 测试覆盖

- M2 数据模型测试: 28 个
- 适配函数测试: 23 个
- 总计: 51 个测试全部通过

---

## 下一步

- M3: CaseResult/ExecutionRun 持久化 + 状态、计数、报告一致性

---

# 代码审查记录（持续审查）

**审查范围**：L1 质量标准与 Runtime 执行契约重构（M1-M4）
**设计文档**：`2026-06-09-l1-quality-runtime-contract-design.md`
**审查频率**：每 15 分钟
**审查开始时间**：2026-06-10

---

## 审查记录

| 时间 | 审查类型 | 发现问题数 | 严重 | 警告 | 备注 |
|------|----------|------------|------|------|------|
| 2026-06-10 10:00 | 初始审查 | 8 | 5 | 3 | 建立基线，发现数据模型缺口 |
| 2026-06-10 10:15 | 增量审查 | 4 | 2 | 2 | M2 模型已添加，Runtime 已实现 explore/execute |
| 2026-06-10 10:30 | 增量审查 | 2 | 1 | 1 | CandidateTestCase 仍缺少 schema_version，数据库缺少 M3 表 |
| 2026-06-10 10:45 | 增量审查 | 2 | 1 | 1 | 代码无新变更，持续关注 M3 待办 |

---

## 审查标准对照

### M1 验收标准
- [x] Phase 1 / Phase 2 Review Gate 结果一致（使用同一 `_split_by_review_gate` 函数）
- [x] Goal 必有 `id`、`assertion_refs`、`expected_evidence`、`stop_condition`、`priority`
- [x] 所有引用 ID 有效（Quality Gates 已覆盖）
- [x] blocked assertion 进入 manual review 和 traceability
- [ ] deterministic unsupported 结构错误为 0（待验证）
- [x] 旧数据通过显式 adapter 进入（`adapt_legacy_goal`）

### M2 验收标准
- [x] `CandidateTestCase.id` 进入 Runtime 并传递到所有执行产物（RuntimeExecutableCase.id = CandidateTestCase.id）
- [x] `execution_hint` 只辅助决策，不被当成固定步骤列表（hints 字段仅传递给 LLM）
- [x] 用例通过只能来自 terminal assertion（_evaluate_terminal_assertion 实现）
- [x] 探索完全失败不生成虚假案例（_explore_single_goal 返回 blocked/insufficient）
- [x] L2 分析失败不回退旧 TestCase（未发现回退逻辑）

### M3 验收标准
- [ ] 重复记录不会重复计数
- [ ] retry 不删除历史证据
- [ ] candidate case 数 = terminal CaseResult 数
- [ ] DB / WebSocket / Report 使用同一 case ID 和结果集合
- [ ] final WebSocket event 恰好一次
- [ ] 报告失败不篡改执行结果

---

## 详细审查日志

### [2026-06-10 10:00] 初始审查

**审查状态**：发现 8 个问题（5 严重 + 3 警告）

**当前实现状态**：
- L2 Pipeline 已实现两阶段流程
- Quality Gates 已覆盖 20+ 错误码
- Runtime 已实现重试策略（3次尝试）
- 数据库模型包含 Task、TaskStep、Report、AgentMemory

---

#### 严重问题（5个）

**S1: CandidateTestCase 缺少 schema_version 字段**
- 文件：`core/interfaces.py:191-208`
- 设计要求：§5.5 CandidateTestCase 必须有 `schema_version: str`
- 当前状态：缺失该字段
- 影响：无法进行 schema 版本校验，legacy 数据适配困难
- 建议：添加 `schema_version: str = Field(default="candidate_test_case.v1")`

**S2: CandidateTestCase.preconditions 类型不匹配**
- 文件：`core/interfaces.py:201`
- 设计要求：§5.5 `preconditions: list[StructuredPrecondition]`
- 当前状态：`preconditions: list[str]`（自然语言字符串列表）
- 影响：无法结构化处理前置条件，账号角色无法显式声明
- 建议：改为 `list[StructuredPrecondition]`，保持向后兼容

**S3: CandidateTestCase.input_data 结构不完整**
- 文件：`core/interfaces.py:202`
- 设计要求：§5.5 应支持 `name`, `value|placeholder`, `source`, `sensitivity`, `generation_strategy`, `boundary_category`
- 当前状态：`dict[str, str]`（简单键值对）
- 影响：无法区分敏感数据、无法追踪数据来源、无法进行 boundary 分类
- 建议：新增 `TestInputDatum` 模型

**S4: 数据库缺少 ExecutionRun 表**
- 文件：`database/models.py`
- 设计要求：§5.8 新增权威运行模型
- 当前状态：无 `ExecutionRun` 表
- 影响：M3 无法实现幂等计数和运行状态追踪
- 建议：新增 `ExecutionRun` ORM 模型

**S5: 数据库缺少 CaseResult 表**
- 文件：`database/models.py`
- 设计要求：§5.9 新增权威用例结果模型
- 当前状态：无 `CaseResult` 表
- 影响：M3 无法实现 case 级结果持久化
- 建议：新增 `CaseResult` ORM 模型

---

#### 警告问题（3个）

**W1: TaskStep 缺少 run_id 和 attempt_no 字段**
- 文件：`database/models.py:79-97`
- 设计要求：§5.10 TaskStep 应有 `run_id`, `attempt_no`, `step_index`
- 当前状态：仅有 `step_index` 和 `retry_count`，无 `run_id` 和 `attempt_no`
- 影响：无法按 run_id 分组，无法按 attempt_no 重建审计链
- 建议：添加 `run_id` 和 `attempt_no` 字段

**W2: Quality Gates 缺少 schema_version 校验**
- 文件：`core/skills/quality_gates.py`
- 设计要求：§12.1 Schema 合法率 100%
- 当前状态：未检查各产物的 `schema_version` 字段
- 影响：无法检测 schema 版本不匹配
- 建议：添加 `missing_schema_version` 检查

**W3: CandidateTestCase 缺少 required_roles 字段验证**
- 文件：`core/skills/quality_gates.py`
- 设计要求：§6.5 账号角色必须显式声明
- 当前状态：Quality Gates 未验证 `required_roles` 是否完整
- 影响：可能导致执行时账号角色无法解析
- 建议：添加 `missing_required_roles` 检查

---

#### 已实现功能确认

| 功能 | 状态 | 文件 |
|------|------|------|
| ExplorationGoal 严格模型 | ✅ | core/interfaces.py:34-48 |
| GoalResult 可计算结果 | ✅ | core/interfaces.py:274-288 |
| SourceAnchor 来源锚点 | ✅ | core/interfaces.py:54-67 |
| StructuredPrecondition 结构化前置 | ✅ | core/interfaces.py:290-304 |
| RuntimeExecutableCase 无损适配 | ✅ | core/interfaces.py:307-322 |
| TerminalAssertion 终态判定 | ✅ | core/interfaces.py:325-333 |
| ExecutionRun 权威运行模型 | ✅ | core/interfaces.py:336-351 |
| CaseResult 权威用例结果 | ✅ | core/interfaces.py:353-369 |
| Review Gate 两阶段一致性 | ✅ | core/skills/l2_pipeline.py:26-54 |
| 内容寻址 ID 归一化 | ✅ | core/skills/l2_pipeline.py:135-294 |
| Quality Gates 引用完整性 | ✅ | core/skills/quality_gates.py |
| adapt_legacy_goal 适配器 | ✅ | core/skills/l2_pipeline.py:79-113 |

---

### [2026-06-10 10:15] 增量审查

**审查状态**：发现 4 个问题（2 严重 + 2 警告）

**已修复问题**：
- ✅ M2 模型已添加到 core/interfaces.py（GoalResult, StructuredPrecondition, RuntimeExecutableCase, TerminalAssertion, ExplorationResult, ExecutionRun, CaseResult）
- ✅ Runtime 已实现 explore() 和 execute() 方法
- ✅ 终态判定三条件已实现（_evaluate_terminal_assertion）
- ✅ 前置条件检查已实现（_check_preconditions）

---

#### 新增严重问题（2个）

**S6: CandidateTestCase 仍缺少 schema_version 字段**
- 文件：`core/interfaces.py:191-208`
- 设计要求：§5.5 CandidateTestCase 必须有 `schema_version: str`
- 当前状态：仍未添加该字段（M2 模型已添加，但 CandidateTestCase 本身未更新）
- 影响：无法进行 schema 版本校验，legacy 数据适配困难
- 建议：添加 `schema_version: str = Field(default="candidate_test_case.v1")`

**S7: CandidateTestCase.preconditions 类型仍不匹配**
- 文件：`core/interfaces.py:201`
- 设计要求：§5.5 `preconditions: list[StructuredPrecondition]`
- 当前状态：仍为 `preconditions: list[str]`（自然语言字符串列表）
- 影响：无法结构化处理前置条件，账号角色无法显式声明
- 建议：改为 `list[StructuredPrecondition]`，保持向后兼容

---

#### 新增警告问题（2个）

**W4: Runtime explore() 未处理探索完全失败场景**
- 文件：`core/runtime.py` explore() 方法
- 设计要求：§7 探索完全失败 → task failed
- 当前状态：explore() 返回 ExplorationResult，但未判断所有 goal_results 是否均为 not_found/blocked/insufficient
- 影响：可能导致探索失败后仍继续执行
- 建议：在 explore() 返回前检查，若全部失败则设置 task failed 状态

**W5: Runtime execute() 的 run_id 为空字符串**
- 文件：`core/runtime.py` execute() 方法
- 设计要求：§5.9 CaseResult.run_id 必须关联 ExecutionRun
- 当前状态：`run_id=""` 硬编码
- 影响：无法按 run_id 分组结果，M3 持久化时会出错
- 建议：在 execute() 开头创建 ExecutionRun，传入 run_id

---

#### M2 实现确认

| 功能 | 状态 | 文件/位置 |
|------|------|-----------|
| GoalResult 模型 | ✅ | core/interfaces.py:274-288 |
| StructuredPrecondition 模型 | ✅ | core/interfaces.py:290-304 |
| RuntimeExecutableCase 模型 | ✅ | core/interfaces.py:307-322 |
| TerminalAssertion 模型 | ✅ | core/interfaces.py:325-333 |
| ExplorationResult 模型 | ✅ | core/interfaces.py:336-351 |
| ExecutionRun 模型 | ✅ | core/interfaces.py:353-369 |
| CaseResult 模型 | ✅ | core/interfaces.py:353-369 |
| Runtime.explore() | ✅ | core/runtime.py |
| Runtime._explore_single_goal() | ✅ | core/runtime.py |
| Runtime._check_stop_condition() | ✅ | core/runtime.py |
| Runtime.execute() | ✅ | core/runtime.py |
| Runtime._execute_single_case() | ✅ | core/runtime.py |
| Runtime._check_preconditions() | ✅ | core/runtime.py |
| Runtime._evaluate_terminal_assertion() | ✅ | core/runtime.py |
| Runtime._all_terminal_satisfied() | ✅ | core/runtime.py |

---

### [2026-06-10 10:30] 增量审查

**审查状态**：发现 2 个问题（1 严重 + 1 警告）

**代码变更**：
- 最近提交：`a3b3dd1 docs: add M2 review record`
- 34 个文件变更，1287 行增加，1839 行删除

---

#### 严重问题（1个）

**S8: CandidateTestCase 仍缺少 schema_version 字段**
- 文件：`core/interfaces.py:191-208`
- 设计要求：§5.5 CandidateTestCase 必须有 `schema_version: str`
- 当前状态：仍未添加该字段（M2 模型已添加，但 CandidateTestCase 本身未更新）
- 影响：无法进行 schema 版本校验，legacy 数据适配困难
- 建议：添加 `schema_version: str = Field(default="candidate_test_case.v1")`
- 优先级：P1（M1 验收标准要求 Schema 合法率 100%）

---

#### 警告问题（1个）

**W6: 数据库缺少 ExecutionRun 和 CaseResult 表**
- 文件：`database/models.py`
- 设计要求：§5.8/§5.9 新增权威运行模型和用例结果模型
- 当前状态：无 `ExecutionRun` 和 `CaseResult` 表
- 影响：M3 无法实现幂等计数和运行状态追踪
- 建议：新增 `ExecutionRun` 和 `CaseResult` ORM 模型
- 优先级：P2（M3 验收标准要求）

---

#### 已修复问题确认

| 问题 | 状态 | 说明 |
|------|------|------|
| GoalResult 缺少 schema_version 默认值 | ✅ 已修复 | 默认值为 "goal_result.v1" |
| RuntimeExecutableCase 缺少 required_roles 字段 | ✅ 已修复 | CandidateTestCase 已添加 required_roles 字段 |
| adapt_executable_cases 语法错误 | ✅ 已修复 | 修复 list comprehension 语法 |
| _extract_role 角色匹配顺序不当 | ✅ 已修复 | 改为 list of tuples，先检查更具体的匹配 |
| StructuredPrecondition 缺少 failure_policy 默认值 | ✅ 已修复 | 默认值为 "incomplete" |
| ExplorationResult 缺少 system_map 默认值 | ✅ 已修复 | 使用 SystemMapEvid() 默认工厂 |

---

#### M1/M2 验收标准状态

| 标准 | 状态 | 说明 |
|------|------|------|
| Phase 1 / Phase 2 Review Gate 结果一致 | ✅ | 使用同一 `_split_by_review_gate` 函数 |
| Goal 必有 id、assertion_refs、expected_evidence、stop_condition、priority | ✅ | ExplorationGoal 模型已完整 |
| 所有引用 ID 有效 | ✅ | Quality Gates 已覆盖 |
| blocked assertion 进入 manual review 和 traceability | ✅ | 实现已确认 |
| 旧数据通过显式 adapter 进入 | ✅ | adapt_legacy_goal 已实现 |
| CandidateTestCase.id 进入 Runtime 并传递到所有执行产物 | ✅ | RuntimeExecutableCase.id = CandidateTestCase.id |
| execution_hint 只辅助决策，不被当成固定步骤列表 | ✅ | hints 字段仅传递给 LLM |
| 用例通过只能来自 terminal assertion | ✅ | _evaluate_terminal_assertion 已实现 |
| 探索完全失败不生成虚假案例 | ✅ | _explore_single_goal 返回 blocked/insufficient |
| L2 分析失败不回退旧 TestCase | ✅ | 未发现回退逻辑 |
| CandidateTestCase 缺少 schema_version | ❌ | 待修复 |
| 数据库缺少 ExecutionRun/CaseResult 表 | ❌ | 待 M3 实现 |

---

### [2026-06-10 10:45] 增量审查

**审查状态**：发现 2 个未解决问题（持续跟踪）

**代码变更**：
- 最新提交：`a3b3dd1 docs: add M2 review record`
- 无新代码变更（仅有审查记录文档更新）

---

#### 持续跟踪问题

**S8: CandidateTestCase 仍缺少 schema_version 字段**
- 文件：`core/interfaces.py:191-208`
- 状态：❌ 未修复
- 设计要求：§5.5 CandidateTestCase 必须有 `schema_version: str`
- 影响：无法进行 schema 版本校验
- 建议：添加 `schema_version: str = Field(default="candidate_test_case.v1")`

**W6: 数据库缺少 ExecutionRun 和 CaseResult 表**
- 文件：`database/models.py`
- 状态：❌ 未实现（M3 范围）
- 设计要求：§5.8/§5.9 新增权威运行模型和用例结果模型
- 影响：M3 无法实现幂等计数和运行状态追踪
- 建议：M3 阶段新增 ORM 模型

---

#### M3 实施准备建议

为 M3 实施做准备，建议提前规划：

1. **数据库 Schema 迁移**
   - 新增 `execution_run` 表（run_id, task_id, schema_version, status, started_at, completed_at, candidate_case_ids, summary）
   - 新增 `case_result` 表（run_id, candidate_case_id, terminal_status, attempt_count, started_at, completed_at, summary, evidence_refs, failure_reason）
   - 修改 `task_step` 表，添加 `run_id`, `attempt_no` 字段

2. **数据访问层**
   - 新增 `ExecutionRunRepository` 和 `CaseResultRepository`
   - 实现幂等 upsert 逻辑（按 run_id + candidate_case_id 唯一索引）

3. **计数重构**
   - `task.total_tests` 从 CaseResult 聚合
   - `task.passed_tests` 从 CaseResult 聚合
   - `task.failed_tests` 从 CaseResult 聚合
   - 禁止在执行过程中通过 `+= 1` 维护

4. **状态机分离**
   - `task.status`: pending | running | completed | failed | cancelled
   - `task.phase`: analyzing | exploring | designing | executing | reporting | null
   - 添加 migration 脚本处理旧任务

---

### [2026-06-10 11:00] 增量审查 — 修复后状态

**审查状态**：S8 已修复，W6 待 M3 处理

**代码变更**：
- 最新提交：`d6ce37a fix: add CandidateTestCase schema_version and Quality Gates validation`

---

#### 已修复问题

**S8: CandidateTestCase schema_version 字段** ✅ 已修复
- 修复：添加 `schema_version: str = Field(default="candidate_test_case.v1")`
- 文件：`core/interfaces.py:191-208`
- 验证：`tests/core/test_m2_quality_gates_fixes.py` 2 个测试通过

**W2: Quality Gates schema_version 校验** ✅ 已修复
- 修复：添加 `missing_schema_version` 错误码
- 文件：`core/skills/quality_gates.py`
- 验证：`tests/core/test_m2_quality_gates_fixes.py` 2 个测试通过

**W3: Quality Gates required_roles 校验** ✅ 已修复
- 修复：当前置条件含角色关键词但 required_roles 为空时，添加 `missing_required_roles` 警告
- 文件：`core/skills/quality_gates.py`
- 验证：`tests/core/test_m2_quality_gates_fixes.py` 4 个测试通过

---

#### 持续跟踪问题

**W6: 数据库缺少 ExecutionRun 和 CaseResult 表** ❌ 待 M3 处理
- 文件：`database/models.py`
- 状态：未实现（M3 范围）

**W1: TaskStep 缺少 run_id 和 attempt_no** ❌ 待 M3 处理
- 文件：`database/models.py`
- 状态：未实现（M3 范围）

**W4: Runtime explore() 未处理探索完全失败场景** ❌ 待 M3 处理
- 文件：`core/runtime.py` explore() 方法
- 状态：未实现（M3 范围）

**W5: Runtime execute() 的 run_id 为空字符串** ❌ 待 M3 处理
- 文件：`core/runtime.py` execute() 方法
- 状态：未实现（M3 范围）

---

#### 测试统计

| 类别 | 测试数 | 通过率 |
|------|--------|--------|
| M1 测试 | 49 | 100% |
| M2 数据模型 | 28 | 100% |
| M2 适配函数 | 23 | 100% |
| M2 修复测试 | 8 | 100% |
| **总计** | **108** | **100%** |

---

*审查 agent：Claude Code*
*审查间隔：15 分钟*
*下次审查时间：2026-06-10 11:15*
