[pytest]
addopts = -p no:cacheprovider
testpaths =
    tests/core
    tests/api
    tests/agents
norecursedirs =
    tests/benchmarks
    tests/l1_output
markers =
    live: requires real browser, network, or model credentials
