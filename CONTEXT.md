# Test Agent

本上下文描述测试设计、受控执行和证据审查中的核心概念。实现代码、存储结构和工具名称不属于本词汇表。

## Language

**测试任务（Test Mission）**：
一次测试工作的范围、目标风险、可用证据、环境边界和明确不测内容。
_Avoid_: Prompt、需求输入

**原始资料（Source Artifact）**：
用户实际提供的文件、链接、源码目录、接口定义、图片或真实系统入口。
原件必须保留内容、类型、版本、hash 和来源，不能被拼接文本替代。
_Avoid_: Prompt上下文、背景文字

**解析资料（Parsed Artifact）**：
解析器从一份原始资料中产生的结构化块集合。每个块保留原始结构和稳定定位，
例如页码、标题路径、表格/单元格、接口路径、文件行号或页面元素。
_Avoid_: 全文字符串、摘要

**解析保真报告（Parse Fidelity Report）**：
证明输入结构是否被解析、哪些内容完整、哪些部分降级或失败的机器可检查报告。
报告不通过时不得进入业务理解、人工确认或用例生成。
_Avoid_: 上传成功、AI说已阅读

**证据声明（Evidence Claim）**：
带来源和定位信息的最小事实或观察，尚未自动成为业务规范。
_Avoid_: Fact、确定需求

**规范业务模型（Normative Business Model）**：
经授权人员确认的业务实体、状态、规则、流程和不变量。
_Avoid_: 页面模型、运行图

**观察系统模型（Observed System Model）**：
从真实系统、接口或运行记录中形成的描述性模型，只表示当前观察到的行为。
_Avoid_: 业务规范、正确模型

**模型组合（Model Portfolio）**：
一个业务切片为表达不同风险而采用的一组用途单一测试模型及其版本关系。
_Avoid_: 万能模型、业务全图

**覆盖准则（Coverage Criterion）**：
针对指定模型版本声明的覆盖分母、强度、排除项和停止条件。
_Avoid_: Technique标签、覆盖策略文本

**覆盖义务（Coverage Obligation）**：
由覆盖准则对模型元素确定性编译出的、必须覆盖或明确豁免的可追踪测试责任。
_Avoid_: CoverageItem、测试点描述

**原子测试规格（Atomic Test Spec）**：
从少量覆盖义务投影出的独立测试意图，包含前置语义、步骤和可单独判断的Oracle。
_Avoid_: CandidateTestCase、浏览器脚本

**链路套件（Chain Suite）**：
为验证业务闭环而按顺序组织的一组原子测试规格，每个检查点仍可独立判断。
_Avoid_: 一个超长用例、端到端大Case

**执行合同（Execution Contract）**：
从冻结测试规格生成的执行约束，规定必做、禁止、渠道、证据、停止和恢复边界。
_Avoid_: Prompt规则、执行建议

**运行知识（Run Knowledge）**：
运行证据、临时缓存、测试修复和候选经验的分层集合，不自动成为业务规范。
_Avoid_: Memory、自动学习规则

## Current Validation Slice

**首个验证切片（First Validation Slice）**：
阿勒泰绩效任务全生命周期：
`自评 → 上级评分 → 确认 → 退回 → 重新评分 → 完成 → 申诉/撤销`。
该切片用于验证状态迁移、多角色权限、条件分支、跨模块消息与统计 Oracle，
并作为 v1/v2 用例生成盲测的第一份固定样本。

该切片不是通用模型模板，也不代表 v2 只支持状态机；它是用于迫使模型合同、
覆盖编译和用例投影接受真实复杂度的首个验证对象。
